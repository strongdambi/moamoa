# from django.db import models

# class Plan(models.Model):
#     created_at = models.DateTimeField(auto_now_add=True)
#     total_amount = models.DecimalField(max_digits=10, decimal_places=2)
#     food_expense = models.DecimalField(max_digits=10, decimal_places=2)
#     transportation_expense = models.DecimalField(max_digits=10, decimal_places=2)
#     savings = models.DecimalField(max_digits=10, decimal_places=2)
#     snack_expense = models.DecimalField(max_digits=10, decimal_places=2)
#     plan_details = models.TextField()
